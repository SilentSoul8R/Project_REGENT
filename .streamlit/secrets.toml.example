# Copy this to `.streamlit/secrets.toml` for local testing (it's gitignored),
# or paste its contents into your Streamlit Cloud app's "Secrets" settings panel.

GROQ_API_KEY = "your_groq_api_key_here"
